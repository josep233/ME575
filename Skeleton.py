
#Choose which stalks to analyze

#Define objective function

#for i in range(0,len(stalks)):
#   initial_guess = avg(matpropranges)
#   constraints = matpropconstraints
#   res = minimize(f,initial_guess,constraints)